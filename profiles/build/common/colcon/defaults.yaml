{
    "test": {
        "abort-on-error": true,
        "return-code-on-test-failure": true,
        "ctest-args": " --output-on-failure ",
        "merge-install": true
    },
    "build": {
        "merge-install": true
    }
}
